import { editionAuth as selectedEditionAuth } from '@edition-implementation/auth'
import type { EditionAuthContract } from '@/lib/edition/contracts/auth'

export const editionAuth: EditionAuthContract = selectedEditionAuth
